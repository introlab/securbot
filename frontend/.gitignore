.DS_Store
node_modules
/dist
/docs

/tests/e2e/reports/
selenium-debug.log

# local env files
# the .env file without the local extension are meant to be commited on the repo
# they can include non sensitive infos that can be pushed and shared (for example, the room name could be in one of them)
# this is how **Vue** classify those files, we should not modify that classification
.env.local
.env.*.local

# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor directories and files
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw*
